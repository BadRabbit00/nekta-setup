---
# Подготовка сервера резервного копирования по документации Nekta:
#   - проверка системных требований
#   - RAID1 из двух HDD и его монтирование
#
# Программное обеспечение резервного копирования документацией не описано
# и здесь не настраивается — см. docs/00-scope.md
- name: Подготовка сервера резервного копирования
  hosts: backup
  become: true
  gather_facts: true

  roles:
    - role: requirements
      tags: [requirements, preflight]
    - role: storage
      tags: [storage]
    - role: firewall
      tags: [firewall]
