# {{ ansible_managed }}
*    soft    nofile    {{ common_nofile_limit }}
*    hard    nofile    {{ common_nofile_limit }}
*    soft    nproc     {{ common_nproc_limit }}
*    hard    nproc     {{ common_nproc_limit }}
root soft    nofile    {{ common_nofile_limit }}
root hard    nofile    {{ common_nofile_limit }}
